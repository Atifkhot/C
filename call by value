#include <stdio.h>

void swap(int x,int y)
{
    printf("\n a=%d\t b=%d",x,y);
    int z;
     z=x;
    x=y;
    y=z;
    
    printf("\nAfter swaping a=%d\t b=%d",x,y);
}
void main()
{
    int a=10;
    int b=20;
    
    printf("Before swaping a=%d\t b=%d",a,b);
   swap(a,b);
}
